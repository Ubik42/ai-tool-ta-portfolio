# 一.问题反馈

用户要求按最新 AI 接入展示审阅结论修改后续开发计划和长期框架，重点是加快开发效率和速度；后续 Maya GUI、截图、录屏、体验类人工测试可以由用户集中处理。本轮只改计划和框架，不进入 R76/R77/R78 的实际功能开发。

# 二.⭐回顾分析

当前稳定基线是 R75：`ai-tool-ta-dcc-first-showcase-r75` / `dcc-first-package@1.72.0`，Presenter Pack 已覆盖 73 个 evidence 和 63 段 route。R76 Control Rig native commandlet probe 已有真实 L3-runtime artifact，但尚未接入 manifest、Presenter Pack 和文档。

最新审阅结论是：工程已经有很强的 DCC / 引擎 deterministic evidence，但 AI 在展示里不够显性。后续讲法应固定为“确定性代码采集、校验、执行、回滚；AI 阅读 evidence、聚类问题、生成 owner handoff、解释风险并建议下一步；AI 输出绑定 evidence refs，不覆盖 deterministic gate”。

# 三.改动解释

已把 `docs/260803_DCC-first长期开发计划与环境.md` 的顶部更新为 R75/R76 现实基线，并新增 R76-R81 队列：R76 收口 commandlet probe，R77 做 `AI Review Packet`，R78 做 Maya `AI Triage`，R79 收口 public package 故事线，R80 清理未引用 artifact，R81+ 再补 MotionBuilder / Houdini / C++ adapter。

已把 `docs/技术报告/260805_长期循环开发框架与轻量验证策略.md` 更新为加速版循环规则：默认按 Tier 0-3 分层验证，开发阶段优先 `py_compile`、`json.tool`、target smoke、`git diff --check`，重型 Unreal BuildPlugin、GUI media 和全量 DCC runtime 只在相关源码或 contract 变化时运行。

已把 `docs/AI_HANDOFF.md` 和 `README.md` 的入口说明改成 AI 展示优先，避免后续 AI 继续被旧的 R47 / MotionBuilder 默认入口带偏。

# 四.计划&状态

后续最短路径：

1. R76：正式接入已有 Control Rig native commandlet probe，不重复做完整 native build。
2. R77：新增 `AI Review Packet`，第一版用 deterministic summarizer 输出可审计草稿，预留真实 LLM 接入字段。
3. R78：在 Maya AuroraView Host 增加 `AI Triage` 面板，展示 blocked 原因、owner 分派、AI 草稿和 evidence refs。
4. R79：统一 README、public package、Presenter Pack 叙事。
5. R80：清理未引用旧 artifact。

当前没有启动实际开发；只完成计划和框架调整。未跟踪 artifact 仍保持未提交，等待 R80 按 manifest / Presenter Pack 引用关系清理。
