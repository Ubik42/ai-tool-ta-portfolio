# 一.问题反馈

长期循环任务继续推进。上一轮已把 Maya `Inspect` 回读结果接入 `Asset Protocol Workbench`，本轮需要把 active payload、scene inspect、diff、validation 合成可交付证据，而不是只停留在界面显示。

# 二.⭐回顾分析

作品集的 DCC-first 展示不能只证明“按钮能调用 Maya”。更强的证据链应该包含：

- 当前业务 payload。
- Maya scene 实际写入/回读结果。
- scene payload 与 editor payload 的 match / drift 判断。
- 规则 validation 结果。
- audit trail 和 diff。
- 可下载 JSON 证据包。

因此本轮把 R9.2 从“可写入/可回读”推进到“可导出 DCC evidence report”。

# 三.改动解释

改动：

- `showcases/portfolio-site/src/components/AssetProtocolWorkbench.tsx`
  - 新增 `dccEvidenceGate`。
  - 新增 `dccEvidenceReport`，版本为 `asset-protocol-dcc-evidence@1.0.0`。
  - 聚合 `editorReport`、`activePayload`、`sceneEvidence`、`validationEvidence`。
  - 新增 `Export DCC Evidence` 下载按钮。
  - 将 scene payload diff 从 10 条预览扩展为完整 report 数据，UI 仍只展示前 10 条。
- `showcases/portfolio-site/src/styles.css`
  - 新增 `DCC Evidence Report` 面板、gate chip、summary grid、evidence code 样式。

文档更新：

- `docs/260803_DCC-first长期开发计划与环境.md`
- `dcc-hosts/maya-auroraview-host/README.md`
- `README.md`

# 四.计划&状态

已验证：

- `npm run build` 通过。
- Maya host Python `py_compile` 通过。
- Maya 2024 `mayapy` smoke 通过：写入 payload、inspect 回读、导出 evidence report 形态 JSON。

生成 report：

```text
<repo>\dcc-hosts\maya-auroraview-host\artifacts\r9-2-dcc-evidence-report-smoke-20260803-154521.json
```

下一轮推荐：

1. 进入 R9.3，把 `Cross-DCC Rule Matrix` 接入 Maya collect / validate / fix preview。
2. 在 Maya GUI 内验证 `Export DCC Evidence` 下载文件内容。
3. 为 R9.3 准备 Maya scene collector API，优先收集 transform / mesh / custom attr / material 基础事实。
