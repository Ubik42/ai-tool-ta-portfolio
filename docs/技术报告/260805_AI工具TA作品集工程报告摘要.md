# AI工具TA作品集工程报告摘要

## 当前发布状态

`AIToolTA_Portfolio` 当前已形成 DCC-first 作品集基线：Maya 2024 内通过 AuroraView 打开工具入口，React/TypeScript 仍作为嵌入式界面层，核心证据来自 DCC / 引擎 runtime、fixture、JSON artifact 和 Presenter Pack。

当前公开包为 `ai-tool-ta-dcc-first-showcase-r40` / `dcc-first-package@1.37.0`。R40 Presenter Pack 已收束 38/38 个关键证据文件、29 步 demo route、5 个主工具模块、Animation Continuity Maya L3、Unreal Animation Bridge import L3、Character Calibration Maya L3、Character Calibration Drilldown L3-derived、Unreal Control Rig Bridge L3、Spatial Authoring Maya L3、Spatial Authoring Drilldown L3-derived、Unreal Socket Import Checker L3、Unreal Socket Authoring Executor API-limited L3、Platform Variant Forge L3-linked、Platform Variant Unreal Runtime Probe L3、Platform Variant Generation Planner L3-derived、Platform Variant Texture Runtime Collector L3、Platform Variant Public Texture2D Payload Fixture L3、Platform Variant Controlled Executor L3 Ready、Platform Variant Executor Expansion Receipts L3-derived Review、Platform Variant StaticMesh Post-check L3 Review、Blender/3ds Max runtime L3、Unreal L3++ 和 Maya 场景事务证据；媒体门禁仍为 `CapturePending`，需要补 Maya GUI 截图和路线录屏。

## 可写入简历的项目

1. **AI Tool TA DCC-first 作品集**

   Maya 2024 + AuroraView + React/TypeScript 的 DCC 内嵌工具展示入口。价值不在网页壳，而在把 Asset Protocol、Rule Matrix、Visual Review、Texture Delivery、Task Orchestrator、Scene Transaction Guard 串成可演示、可导出、可审计的工具管线案例。

2. **跨 DCC / 引擎资产检查适配器**

   把 Lightbox 类 Pyblish 资产检查经验抽象成公开 fixture 和 rule contract。当前覆盖 Blender `bpy` L3、3ds Max `pymxs` L3、Unreal L3++ handoff inspector：关注 user properties、LOD、材质槽、UV、transform、collision、引擎 registry、平台 preset 和 waiver 边界。

3. **Maya 资产交付工具模块组**

   面向真实 TA 发布流程：协议写入与回读、规则矩阵、截图清单、贴图交付检查、dry-run 任务队列、资产放行决策、引擎导入 preflight、scene transaction diff 和 rollback preview。展示点是“工具修改了什么、为什么能放行、哪里必须阻断”。

4. **Animation Continuity / Unreal Animation Bridge**

   面向动画交付的业务正确性检查：Maya `mayapy` 创建并采集 keyed animCurve facts，把 rig identity、skeleton fingerprint、take range、sample rate、required channel、sub-frame key、root motion、scale drift 和 additive layer 变成可审计证据；Unreal Python 继续完成 Maya FBX 生成、AnimSequence / Skeleton 导入和 runtime facts 采集。

5. **Character Calibration / Spatial Authoring**

   面向角色和空间挂点交付：Maya `mayapy` 采集 topology、joint coverage、face params、Control Rig mapping，以及 socket parent joint、mirror pair、hotspot semantic、pose frame、local-space 和 pose transfer approval；R35/R36 已分别把角色和空间作者 L3 flat rows 投影成可在 Maya/AuroraView 展示的 drilldown panels、owner actions 和 fix previews，R37 继续把角色 Control Rig mapping 接到 Unreal 5.3.2 API / SkeletalMesh / Skeleton / Control Rig asset readiness；R38 把 Spatial Authoring drilldown 接到 Unreal SkeletalMesh / Skeleton socket API 和 expected socket coverage；R40 通过 controlled executor 证明 UE 5.3 Python 无法可靠写入 socket identity，形成正确的 API-limited gate。

6. **Platform Variant Forge**

   面向 PC -> Mobile 资产派生：把目标路径、owner approval、triangle/texture/material/draw-call budget、LOD coverage、Nanite、shader feature、collision policy、Unreal runtime facts、材质贴图依赖事实和 public Texture2D payload 合成可审计 variant plan，并把 drift 转成 dry-run generation operations。展示点是“平台降级为什么能自动做、哪里必须阻断、引擎里实际是否达标、下一步生成动作如何受控执行”。

7. **MayaCraft AI 友好模块化绑定框架**

   独立探索面向 AI agent 的 Maya rig/tool 框架，目标是把绑定组件、参数协议、执行入口和模块组合方式结构化，方便后续由 agent 读取、调用和扩展。

## 验证证据

- Maya Host / AuroraView：用户已在 Maya 中验证 `show_portfolio()` 可以弹出界面。
- Presenter Pack：`r40-unreal-socket-authoring-executor-presentation-pack-20260805-222519.json`，38/38 evidence files present，0 missing required files，29 demo route steps。
- Character Calibration Drilldown：`character-calibration-drilldown-20260805-202259.json`，L3-derived Blocked；2 character drilldowns，14 panels，8 issue rows，8 owner actions，6 owner-required，2 manual-review，productionWrites=0。
- Unreal Control Rig Bridge：`unreal-control-rig-bridge-l3-20260805-205656.json`，Unreal 5.3.2 Python L3 Blocked；Control Rig API ready true，2 character rows，0 Ready / 0 Review / 2 Blocked，8 pass / 1 warning / 7 error，skeletal bindings / Control Rig assets = 1 / 0，assetWrites=0。
- Spatial Authoring Drilldown：`spatial-authoring-drilldown-20260805-203713.json`，L3-derived Blocked；2 spatial drilldowns，18 panels，9 issue rows，9 owner actions，7 owner-required，2 manual-review，productionWrites=0。
- Unreal Socket Import Checker：`unreal-socket-import-checker-l3-20260805-212131.json`，Unreal 5.3.2 Python L3 Blocked；2 spatial rows，0 Ready / 0 Review / 2 Blocked，9 pass / 2 warning / 9 error，socket API ready，expected/runtime sockets = 4 / 0，assetWrites=0。
- Unreal Socket Authoring Executor：`unreal-socket-authoring-executor-20260805-222014.json`，Unreal 5.3.2 Python L3 Blocked；selected/held rows = 1 / 1，expected/created sockets = 2 / 0，9 pass / 0 warning / 2 error，assetWrites=0，原因是 commandlet-created `SkeletalMeshSocket.socket_name` 和 `bone_name` 为 read-only。
- Unreal Socket API docs probe：`unreal-socket-api-docs-20260805-222200.json`，记录 `SkeletalMesh.add_socket(socket, add_to_skeleton=False)` 可见，但 socket identity 字段不可写，避免误判为可安全自动修复。
- Platform Variant Forge：`platform-variant-forge-contract-20260805-183315.json`，L3-linked 到 Unreal preset fact comparison，2 source assets / 3 variants，2 Ready / 1 Blocked；21 pass / 1 warning / 8 error。
- Platform Variant Unreal Runtime Probe：`platform-variant-unreal-runtime-20260805-185026.json`，Unreal 5.3.2 Python L3，3 runtime variants，0 Ready / 2 Review / 1 Blocked；21 pass / 4 warning / 2 error。
- Platform Variant Generation Planner：`platform-variant-generation-plan-20260805-190052.json`，L3-derived，11 operations，1 Ready / 3 Review / 2 Blocked / 5 Satisfied；每个 operation 带 deterministic params、Unreal Python preview、writeSet、rollback 和 owner approval 边界。
- Platform Variant Texture Runtime Collector：`platform-variant-texture-runtime-20260805-191529.json`，Unreal 5.3.2 Python L3，3 runtime variants，1 Ready / 1 Review / 1 Blocked；19 pass / 1 warning / 1 error；Mobile Review 来自 synthetic material 没有真实 Texture2D payload。
- Platform Variant Public Texture2D Payload Fixture：`platform-variant-texture-payload-runtime-20260805-193515.json`，Unreal 5.3.2 Python L3，生成并导入 public 2048 Texture2D，挂到 `M_HeroPanel` 后重算预算；3 runtime variants，2 Ready / 0 Review / 1 Blocked；20 pass / 0 warning / 1 error；最终幂等 rerun assetWrites=0，写入只限 public fixture。
- Platform Variant Controlled Executor：`platform-variant-controlled-executor-20260805-200810.json`，Unreal 5.3.2 Python L3 Ready，执行 public Texture2D max-size clamp `0 -> 2048`，post-check 通过，再 rollback 到原 fingerprint；7 pass / 0 warning / 0 error，assetWrites=2，persistentMutation=false。
- Platform Variant Executor Expansion Receipts：`platform-variant-executor-expansion-20260805-201222.json`，L3-derived Review，把 LOD/Nanite/collision operation 转成 5 条 approval / rollback receipts；2 no-op verified，1 approval-ready，2 readiness-only，0 blocked，productionWrites=0。
- Platform Variant StaticMesh Post-check：`platform-variant-staticmesh-postcheck-20260805-215500.json`，Unreal 5.3.2 Python L3 Review，把 R34 LOD/Nanite/collision receipts 对照到只读 StaticMesh runtime facts；2 target assets present，2/2 no-op matched，3 owner-held，32 pass / 3 warning / 0 error，assetWrites=0。
- Spatial Authoring：Maya 2026 `mayapy` L3，2 spatial authoring rows，1 Ready / 1 Blocked；11 pass / 2 warning / 7 error；runtime collected true。
- Unreal Animation Bridge：Unreal 5.3.2 Python import L3，2/2 expected AnimSequence rows present，4 synthetic assets imported，1 Ready / 0 Review / 1 Blocked；12 pass / 1 warning / 5 error；writes limited to public synthetic test project。
- Character Calibration：Maya 2026 `mayapy` L3，2 character rows，1 Ready / 1 Blocked；10 pass / 2 warning / 6 error；runtime collected true。
- Animation Continuity：Maya 2026 `mayapy` L3 2 assets，1 Ready / 1 Blocked；11 pass / 3 warning / 6 error；runtime collected true。
- Unreal：L3++ 读取 Unreal 5.3.2 registry 和 StaticMesh engine facts，source import、material slot、LOD、collision 4/4 matched。
- 3ds Max：`pymxs` L3 2 assets，1 Ready / 1 Blocked；13 pass / 5 warning / 2 error；runtime collected true，object count 4。
- Blender：`bpy` L3 2 assets，1 Ready / 1 Blocked；8 pass / 3 warning / 1 error；Blender 5.2.0 LTS background。
- Scene Transaction Guard：Maya 内 before/after fingerprint、created/deleted/modified 2/2/2、rollback actions 9、risk rows 4。

## 下一步

简历展示优先写“DCC/引擎工具管线能力”，不要写成普通前端项目。下一阶段最有价值的补强是两件事：补 Unreal AnimSequence curve/compression/runtime 细节；或做 public Control Rig asset fixture / runtime hierarchy。socket 自动写入如果继续做，应换 Unreal C++ / Editor Utility Blueprint adapter。Maya GUI 截图/录屏留到最后集中采集。


## R39 补充

- Presenter Pack：`r39-platform-variant-staticmesh-postcheck-presentation-pack-20260805-215900.json`，36/36 evidence files present，0 missing required files，28 demo route steps。
- 新增证据：`<repo>\dcc-hosts\platform-variant-forge\artifacts\platform-variant-staticmesh-postcheck-20260805-215500.json`，验证 R34 LOD / Nanite / collision receipts 与 Unreal StaticMesh runtime facts 的一致性。

## R40 补充

- Presenter Pack：`r40-unreal-socket-authoring-executor-presentation-pack-20260805-222519.json`，38/38 evidence files present，0 missing required files，29 demo route steps。
- 新增证据：`<repo>\dcc-hosts\unreal-socket-import-checker\artifacts\unreal-socket-authoring-executor-20260805-222014.json` 和 `<repo>\dcc-hosts\unreal-socket-import-checker\artifacts\unreal-socket-api-docs-20260805-222200.json`，证明 Unreal socket authoring 的 Python API 边界。
